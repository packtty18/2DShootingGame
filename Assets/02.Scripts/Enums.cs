public enum EItemType
{
    MoveSpeedUp,
    HealthUp,
    AttackSpeedUp
}

public enum EEnemyType
{
    Direction,
    Trace,
    Teleport
}

public enum EPlayerAIStateType
{
    Idle,
    Attack,
    Retreat
}